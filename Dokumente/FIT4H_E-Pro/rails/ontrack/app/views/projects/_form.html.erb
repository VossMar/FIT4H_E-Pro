<%= form_for (@project) do |p| %>

	<%= p.label :titel %>
	<%= p.text_field :title %>

	<%= p.label :description %>
	<%= p.text_area :description %>
	
	<%= p.label :start_date %>
	<%= p.datetime_select :start_date %>

	<%= p.submit "Übernehmen", class: "button" %>

<% end %>
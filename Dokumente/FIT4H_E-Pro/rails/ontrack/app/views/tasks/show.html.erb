<p id="notice"><%= notice %></p>


<p>
  <strong>Title:</strong>
  <%= @task.title %>
</p>

<p>
  <strong>Description:</strong>
  <%= @task.description %>
</p>

<p>
  <strong>Start date:</strong>
  <%= @task.start_date %>
</p>

<p>
  <strong>End date:</strong>
  <%= @task.end_date %>
</p>

<p>
  <strong>Project:</strong>
  <%= @task.project_id %>
</p>

<%= link_to 'Edit', edit_project_task_path(@task.project_id, @task.id), class: "button" %>
<%= link_to 'Destroy', project_task_path(@task.project_id,@task.id), method: :delete, class: "button" %>
<%= link_to 'Back', project_tasks_path(@project.id), class: "button" %>
